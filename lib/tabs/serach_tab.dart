import 'package:flutter/material.dart';

class SerachTab extends StatelessWidget {
  const SerachTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}